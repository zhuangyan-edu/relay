---
name: domain-skill-template
description: 本项目专属领域技能模板。触发方式：显式输入 /domain-cmd 或口语调用；隐式当进行特定领域任务、修改特定契约或触发特定工程红线时自动激活。
trigger_mode: hybrid
command: /domain-cmd
---

# [领域技能名称]: 项目专有工程与业务规范

> **💡 JIT 渐进式按需载入机制 (Progressive Disclosure)**：
> 本文件是本项目独有的外部物理规约资产。平时**绝不常驻注入系统提示词**（常驻消耗 0 Token）；
> 只有当被显式命令（如 `/domain-cmd`）呼叫，或隐式命中上述激活场景时，AI 才会动态查阅本文件并按规约执行，用完即退。

---

## 1. 领域认知与物理红线 (Domain Invariants & Redlines)
<!-- 在此定义该领域/模块不可动摇的底线，例如特定 API 契约前置、特定数据格式校验、禁止使用的反模式等 -->
* **红线一**：[例如：修改接口前必须先更新 openapi 契约文件]
* **红线二**：[例如：数据落盘前必须通过 schema 校验，严禁写入脏数据]

---

## 2. 标准作业流程 (Step-by-Step Workflow)
<!-- 定义 AI 在执行此类任务时的标准流程式指引 -->
1. **输入分析**：解析用户输入与当前工程现状；
2. **规约核验**：对照本技能的领域红线自检方案可行性；
3. **分阶段执行**：
   - 涉及重大改动时，必须遵循“确认先行”协议，先呈送实施清单；
   - 获得用户授权后，方可执行物理文件修改；
4. **验证闭环**：运行专项校验脚本或单元测试，确保改动真实生效。

---

## 3. 产出物物理归位 (Asset Placement)
* 业务文档/PRD ➜ 存入 `docs/`（100% 留给业务用户）
* 临时排错草稿 ➜ 存入 `scratch/`（用完可清理）
* 关键技术沉淀 ➜ 依照 `/cd` 流程登记至 `.agents/cd-weekly-log.md`
